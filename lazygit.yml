keybinding:
  universal:
    # subviews / tabs within a panel (the original binding that works)
    prevTab: 'h'
    nextTab: 'l'
    prevBlock-alt: <disabled>
    nextBlock-alt: <disabled>
    # up/down in lists
    prevItem-alt: 'k'
    nextItem-alt: 'j'
